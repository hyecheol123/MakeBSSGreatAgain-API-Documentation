tags:
  - Auth
summary: "Health Check: Server Ready"
operationId: authServerReady
description: |
  Check whether the server is ready to handle user's request or not.  
  This API only be accessible through the private network.
responses:
  '200':
    description: Server Ready